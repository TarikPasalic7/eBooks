<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:EKnjige.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="EKnjige.MobileApp.Views.LoginPage">
    <ContentPage.BindingContext>
        <vm:LoginViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content >
        <StackLayout Padding="7"  Spacing="10" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
            <Label Text="Korisničko Ime :" />
            <Entry Text="{Binding KorisnickoIme}"></Entry>
            <Label Text="Password :"/>
            <Entry  Text="{Binding Lozinka}" IsPassword="True"></Entry>
            <Button Command="{Binding LoginCommand}" Text="Login" BackgroundColor="MediumPurple" TextColor="White"></Button>
            <Label IsVisible="{Binding IsBusy}" Text="..."/>
            <Button Command="{Binding RegistracijaCommand}" Text="Registruj se" BackgroundColor="MediumPurple" TextColor="White"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>